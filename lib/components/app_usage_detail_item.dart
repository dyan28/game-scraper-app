import 'package:flutter/material.dart';

class AppUsageDetail extends StatelessWidget {
  const AppUsageDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}